VAULTWARDEN_TZ=Europe/Vienna

VAULTWARDEN_UID=568
VAULTWARDEN_GID=568

# --- Vaultwarden ---
VAULTWARDEN_DOMAIN=https://vault.example.invalid
VAULTWARDEN_WEB_PORT=30032

VAULTWARDEN_DATA_HOST_PATH=./vaultwarden/data
VAULTWARDEN_CPUS=1
VAULTWARDEN_MEM_LIMIT=512m

# Admin token as Argon2 hash:
# docker run --rm -it vaultwarden/server /vaultwarden hash --preset owasp
VAULTWARDEN_ADMIN_TOKEN=change-me

VAULTWARDEN_SIGNUPS_ALLOWED=false
VAULTWARDEN_INVITATIONS_ALLOWED=true

# --- SSO (Pocket ID / OIDC) ---
VAULTWARDEN_SSO_ENABLED=true
# true = hide master-password login, SSO is the only way in
VAULTWARDEN_SSO_ONLY=false
# Links an SSO login to an existing local account with the same email.
# Risk: https://github.com/dani-garcia/vaultwarden/wiki/Enabling-SSO-support-using-OpenId-Connect#on-sso_allow_unknown_email_verification
VAULTWARDEN_SSO_SIGNUPS_MATCH_EMAIL=true
VAULTWARDEN_SSO_ALLOW_UNKNOWN_EMAIL_VERIFICATION=true
# must match the PKCE setting of the Pocket ID client
VAULTWARDEN_SSO_PKCE=true
VAULTWARDEN_SSO_SCOPES=email profile groups offline_access
VAULTWARDEN_SSO_AUTHORITY=https://id.example.invalid
VAULTWARDEN_SSO_CLIENT_ID=change-me
VAULTWARDEN_SSO_CLIENT_SECRET=change-me

# --- PostgreSQL ---
VAULTWARDEN_POSTGRES_DATABASE=vaultwarden
VAULTWARDEN_POSTGRES_USER=vaultwarden
VAULTWARDEN_POSTGRES_PASSWORD=change-me
VAULTWARDEN_POSTGRES_DATA_HOST_PATH=./vaultwarden/postgres-data
VAULTWARDEN_POSTGRES_CPUS=1
VAULTWARDEN_POSTGRES_MEM_LIMIT=512m

# --- SMTP ---
VAULTWARDEN_SMTP_HOST=smtp.example.invalid
VAULTWARDEN_SMTP_PORT=587
VAULTWARDEN_SMTP_SECURITY=starttls
VAULTWARDEN_SMTP_USERNAME=vaultwarden@example.invalid
VAULTWARDEN_SMTP_PASSWORD=change-me
VAULTWARDEN_SMTP_FROM=vaultwarden@example.invalid
VAULTWARDEN_SMTP_FROM_NAME=Vaultwarden
