---
users:
  admin:
    displayname: "Administrator"
    # Passwort-Hash generieren:
    # docker run --rm authelia/authelia:4 authelia crypto hash generate argon2 --password 'dein-passwort'
    password: "HASH_HIER_EINTRAGEN"
    email: admin@example.invalid
    groups:
      - admins
      - users
